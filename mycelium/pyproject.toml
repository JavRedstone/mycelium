[project]
name = "mycelium"
version = "0.1.0"
description = "Autonomous GitLab agent that prevents engineering knowledge loss using Vertex AI, ADK, Gemini, GitLab MCP, and MongoDB MCP"
license = { text = "Apache-2.0" }
requires-python = ">=3.11"
